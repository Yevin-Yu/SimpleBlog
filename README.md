# 耶温博客

一个基于 React + TypeScript + Vite 构建的现代化个人博客网站。

## 特性

- 📝 支持 Markdown 编写博客
- 🎨 炫酷的粒子动画首页
- 📚 简洁的博客目录树
- 💻 代码高亮支持（Highlight.js）
- 📱 响应式设计
- ⚡ 快速构建和热更新（Vite）
- 🔍 **完整 SEO 优化**（Meta 标签、结构化数据、Sitemap）

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **React Router** - 路由管理
- **Markdown-it** - Markdown 解析
- **Highlight.js** - 代码高亮

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173

### 构建生产版本

```bash
npm run build
```

构建产物在 `dist` 目录，可以直接部署到静态服务器。

### 预览构建结果

```bash
npm run preview
```

## 项目结构

```
.
├── src/
│   ├── components/           # 组件
│   │   ├── BlogTreeContent/  # 博客树内容组件
│   │   ├── BlogTreeSidebar/  # 博客树侧边栏组件
│   │   ├── MarkdownRenderer.tsx  # Markdown 渲染器
│   │   └── SEO.tsx           # SEO 组件
│   ├── pages/                # 页面
│   │   ├── Home.tsx          # 首页
│   │   └── BlogTree.tsx      # 博客树页面（列表+详情）
│   ├── hooks/                # 自定义 Hooks
│   │   ├── useBlogTree.ts    # 博客树逻辑
│   │   └── useSiteUrl.ts     # 站点 URL
│   ├── utils/                # 工具函数
│   │   ├── blog.ts           # 博客数据管理
│   │   ├── markdown.ts       # Markdown 处理
│   │   └── sitemap.ts        # 网站地图生成
│   ├── styles/               # 样式变量
│   ├── types/                # 类型定义
│   ├── constants/            # 常量
│   ├── App.tsx               # 主应用
│   └── main.tsx              # 入口文件
├── blogs/                    # Markdown 博客文件目录
├── scripts/                  # 构建脚本
│   └── generate-sitemap.js   # 生成网站地图
└── dist/                     # 构建输出目录
```

## 添加新博客

1. 在 `blogs/` 目录下创建 Markdown 文件
2. 在 `src/utils/blog.ts` 中添加博客配置：
   - 在 `blogList` 数组中添加博客元数据
   - 在 `blogContents` 对象中添加博客内容

## 自定义

### 修改主题颜色

编辑 CSS 文件中的颜色变量：
- 主色调：`#64c8ff`（蓝色）
- 背景色：`#0a0a0a`（深黑）

### 修改首页动画

编辑 `src/pages/Home.tsx` 中的粒子动画参数。

## SEO 优化

项目已实现完整的 SEO 优化：

- ✅ Meta 标签优化（Title, Description, Keywords）
- ✅ Open Graph 和 Twitter Card 标签
- ✅ 结构化数据（Schema.org JSON-LD）
- ✅ 自动生成 sitemap.xml
- ✅ robots.txt 配置
- ✅ 语义化 HTML
- ✅ 移动端优化

详细说明请查看 [SEO.md](./SEO.md)

**重要**：部署前请将代码中的 `https://your-blog-domain.com` 替换为你的实际域名。

## 部署

构建完成后，将 `dist` 目录的内容部署到任何静态文件服务器即可。

### 快速部署

**方式一：Netlify（推荐）**
1. 运行 `npm run build`
2. 访问 [Netlify](https://www.netlify.com/)，将 `dist` 目录拖拽到部署区域
3. 配置自定义域名（可选）

**方式二：Vercel**
```bash
npm i -g vercel
npm run build
vercel --prod
```

**方式三：GitHub Pages**
1. 安装 `gh-pages`: `npm install --save-dev gh-pages`
2. 在 `package.json` 中添加：
   ```json
   {
     "scripts": {
       "deploy": "npm run build && gh-pages -d dist"
     }
   }
   ```
3. 运行 `npm run deploy`

**方式四：自己的服务器（Nginx）**
```bash
# 上传文件
scp -r dist/* user@server:/var/www/html/
```

详细部署指南请查看 [DEPLOY.md](./DEPLOY.md)

### 部署前准备

1. **更新域名配置**：修改 `src/config/index.ts` 中的 `SITE_CONFIG.url`
2. **重新构建**：运行 `npm run build`
3. **验证**：运行 `npm run preview` 检查构建结果

### 部署后

1. 验证所有页面正常访问
2. 在 Google Search Console 提交 sitemap: `https://your-domain.com/sitemap.xml`
3. 验证 SEO meta 标签正确

## License

MIT
